name: hermes
display_name: Hermes
timezone: America/Santiago

mcp:
  servers:
    hermes:
      type: http
      url: http://hermes-mcp:8093/mcp

prompts:
  default: app/prompts/default.md
  whatsapp: app/prompts/whatsapp.md

skills:
  dir: app/skills
  excluded_channels:
    whatsapp: []

agents:
  dir: app/agents

always_allowed_tools: []
