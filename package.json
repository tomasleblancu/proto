{
  "name": "proto",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev:gateway": "PROTO_APP_ROOT=examples/hermes npm run dev -w packages/core-gateway",
    "dev:web": "npm run dev -w packages/core-web",
    "build": "npm run build -w packages/core-shared && npm run build -w packages/core-mcp && npm run build -w packages/core-gateway && npm run build -w packages/core-web",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
