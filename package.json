{
  "name": "hermes",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:gateway": "npm run dev -w packages/gateway",
    "dev:web": "npm run dev -w packages/web",
    "build": "npm run build -w packages/shared && npm run build -w packages/mcp && npm run build -w packages/gateway && npm run build -w packages/web",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
