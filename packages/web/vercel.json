{
  "buildCommand": "cd ../.. && npm install && npm run build -w packages/shared && npm run build -w packages/web",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
