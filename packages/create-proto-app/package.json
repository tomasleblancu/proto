{
  "name": "create-proto-app",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "create-proto-app": "./dist/cli.js"
  },
  "scripts": {
    "bundle": "node scripts/bundle.js",
    "build": "tsc",
    "prepublishOnly": "npm run bundle && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
